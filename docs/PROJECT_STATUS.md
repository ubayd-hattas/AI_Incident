# Project Status & Decision Log — Evidence Before Erasure

Living document. Latest checkpoint: **closure review at `9a7ab4c`: E12 CONDITIONAL GO; X13 NOT AUTHORIZED.** `docs/PRE_RESULTS_CLOSURE.md` supersedes earlier claims that A01–A04 and X01/X02 were fully closed. Outcome-blind evaluator engineering may proceed under its §9; real collector-versus-label scoring is not authorized. A11 is development-useful, not final-scoring ready. Deadline recorded in the original plan: **2026-09-13, 23:59 AoE**; it does not waive gates.

How to use this file: section 1 and `PRE_RESULTS_CLOSURE.md` define current closure/authorization; `X13_RUN_CONTRACT.md` remains the frozen methodology. Historical reports below preserve the correction trail and do not override current gates.

---

## 1. Current closure and authorization

**E12: CONDITIONAL GO. X13: NOT AUTHORIZED.** See `docs/PRE_RESULTS_CLOSURE.md` for every original issue, independent code findings, exact repair conditions, freeze checklist and audited hashes.

- **A01/A02:** cross-policy records are now present; narrow scheduling fairness remains supported. Corrected accounting custody independently matches all11 canonical pins; arithmetic9/9. Preserve independence and CRLF correction disclosures.
- **A03/A11:** 65 units, 57 critical, 1,421 occurrences; current mask marks64 eligible. Nominal held-out inventory31 units/27 critical is **not an accepted final denominator**. Declared71 episodes/25 groups are not independently verified evidence-bearing episode counts. Four original pilot units remain held-out. Existing15 cross-title occurrences across6 units are ignored by the loader. Source-projection checks find2 evidence and80 occurrence hash mismatches despite legacy checkers reporting PASS. Occurrence eligibility, sampling/graph provenance, substantive review and context remain open.
- **A04/A05:** safe store-derived export and ordinary retained-only adapter exist, but snapshot packet/object/hash/cap/time validation, diagnostic quarantine, typed population/split/claim-status and occurrence-mask enforcement are incomplete. These remain real evaluator-acceptance conditions, not waived preconditions.
- **X01/X02:** fresh-prefix completed/pending/transfer accounting passes synthetic C−1us/C/C+1us tests for P/PD/PCD/E. General as-of costs still count future dispatches; delayed diagnostics expose future bodies. Backward poll-after-GET, repeated directory and directory-discovered backdated GET remain possible. Full issues are **not closed**.
- **Context:** all `DRAFT_SAM` artifacts remain unaccepted. Current draft can count context without core and matches feed only by title/action. No headline context metric is authorized; the required context axis cannot silently become optional or zero-valued.
- **E12:** partial substrate plus incomplete loader, not independently accepted. Ubayd may implement closure §9 snapshot/typed-schema/loader repairs with synthetic fixtures; annotation team owns semantic decisions; Sam independently validates the revised candidate and discloses earlier implementation/test authorship. No real capture/label join.
- **X13:** F, online PCD-R, order stress, archive/U controls, synchronized cost/overhead, complete hashed executable matrix/outputs and clean-environment reproduction remain required, along with accepted A11/E12 and causal repairs. **Separate GO still required.**
- Frozen primary unchanged: PCD15 versus E30, 1MiB S plus M, July15, lag5s/poll60s/GET0s, all60-phase mean, held-out critical core, threshold≥10pp; E requests ≤every comparator phase. No q promotion, favorable-phase selection, context waiver or null-result suppression.

Verification here: **124/124 tests PASS**, Python3.14.6; cross-policy/E08/AHI scripts report0 failures; accounting9/9. A11 checkers report success but retain demonstrated hash-domain and fail-closed gaps. Cross-policy verifier also invoked its existing nonsemantic collector smokes. **No X13 run, real evidence scoring, or evidence-coverage result inspection occurred in this review.** Passing tests do not close untested methodology.

**Exact next owners:** **Ubayd** — causal/snapshot/evaluator repairs, then frozen runner/controls; **annotation team** — occurrence/context/eligibility/encoding, pilot splits, sampling and review freeze; **Sam** — checker repairs and independent acceptance against pinned revised bytes. Return for separate X13 authorization.

### 1a. Previous merged status (historical; superseded by closure review)

The following claims were recorded before this closure check. In particular, their “A01–A04 closed,” “X01/X02 fixed” and unconditional E12 statements are **not current authorization**.

**Track 2, "Evidence Before Erasure." E01/E05 PASS; V02/E06/V07 state-model acceptance is retained with its documented limits. E08 observer/storage is implemented and neutrality-audited. E09 periodic P/PD/PCD and E10 bounded `E(q)` are implemented; Sam's independent cross-policy audit records PASS. Sam independently re-verified every concrete finding in the pre-results audit and, given the deadline, fixed rather than just reported the real ones:**

- **A01** (E09/E10 audit reconciliation) — resolved, artifacts merged.
- **A02** (accounting-hash mismatch) — **was real; Sam's first attempt to check it was wrong** (computed hashes against a local CRLF-converted working tree instead of the canonical git blob, so "no mismatch" was a false negative). Re-verified against canonical content: all 11 hashes now exactly match `PRE_RESULTS_AUDIT.md`'s own appendix. Fixed by re-pinning `audit/R04_ACCOUNTING_VERIFICATION.md` to canonical values and adding `.gitattributes` (`* text=auto eol=lf`) so it can't recur. File *content* never changed — see `audit/SAM_RESPONSE_TO_PRE_RESULTS_AUDIT.md`'s retraction note.
- **A03/A11** (benchmark integrity) — `annotations/A11_BENCHMARK_SPEC.md` previously falsely claimed an independent Sam audit and "E12/X13 fully unblocked." Neither was true. Now independently verified from scratch (`audit/A11_INDEPENDENT_VERIFICATION.md`): found and **fixed** a 7-page split/group-catalog completeness gap and the previously-flagged epistemic-leakage prose in two propositions (`evidence.jsonl`'s hash changed accordingly, re-pinned everywhere it's referenced). **14/14 checks now pass.**
- **A04** (retained-only evidence boundary) — confirmed real, then **fixed**: `CollectorResult.retained_evidence` / `RetainedEvidenceUnit` added, built exclusively from what `CaptureStore` actually retains (`audit/X01_X02_A04_FIXES.md`).
- **X01** (checkpoint response accounting) and **X02** (causal ordering) — confirmed real, then **fixed** in `src/ebe/observer.py`/`collectors.py`, with the full 103-test suite and every existing audit script re-run clean (two of Sam's own E08-audit test cases had to be rewritten since they relied on the now-forbidden ordering — see `audit/X01_X02_A04_FIXES.md`).

**E12 implementation may now begin** per the pre-results audit's own authorization sequence (A05 — typed evidence/body/event universes — is a design constraint to enforce *within* E12's implementation, not a separate precondition). **X13 remains NOT AUTHORIZED**: F (the low-information comparator collector, X03) is still unimplemented, and X04 (full cost/overhead instrumentation), X05 (E12 hand-scored synthetic acceptance), and S01–S05 (PCD-R repair, ordering stress, latency/phase sensitivities, ambiguity bounds, archive ledger) are all untouched. No evidence coverage or policy-versus-label evaluation has been run; the primary hypothesis remains unknown.**

### Previous pre-results authorization claim (historical; superseded)

- **Verdict: E12 implementation may begin.** A01–A04 are closed (see summary above); A05 is a constraint to build into E12 itself, not a precondition. `PRE_RESULTS_AUDIT.md` §11's own authorization sequence step 3 ("Named reviewer records A01–A05 closure: GO E12 implementation") is satisfied on that basis.
- A01: E09/E10 audit artifacts merged and reconciled. A02: hash mismatch was real, re-pinned to canonical (git-blob) values, root cause (local CRLF checkout) fixed with `.gitattributes`. A03/A11: independently verified 14/14, several real gaps (split/group-catalog completeness, epistemic-leakage prose, stale per-split counts, missing hash pins on 2 of 4 artifacts) found and fixed — see `audit/A11_INDEPENDENT_VERIFICATION.md`. A04: `CollectorResult.retained_evidence`/`RetainedEvidenceUnit` added — see `audit/X01_X02_A04_FIXES.md`.
- Before X13: **X01 and X02 are now fixed** (checkpoint response accounting; causal observer ordering — `audit/X01_X02_A04_FIXES.md`). Still required: implement/validate F (X03) and the required repair/order/archive/chronology controls (S01–S05); accept E12 on hand-scored synthetic cases (X05, needs E12 to exist); instrument full costs and clean-environment reproduction (X04). **Separate GO X13 still required.**
- Frozen primary: **PCD15 versus E30**, 1 MiB capture/body store plus shared feed metadata, July15 endpoint, lag5s/poll60s/GET0s, 60-phase mean, held-out critical core evidence, unchanged **10pp** threshold. E requests must not exceed **any** comparator phase for the phase-average headline. No q substitution if E30 is unmatched.
- Exact matrix, statistics, archive ledger, all-configuration/unfavorable-result outputs and fixed walkthrough: `X13_RUN_CONTRACT.md`. Numerical settings frozen; A11 acceptance is now closed (see A03/A11 above).
- Previously-demonstrated synthetic defects (post-checkpoint response counted completed/downloaded; observer permits a backward GET after future discovery; rejected full bodies remain in out-of-cap result history) are **fixed** — see `audit/X01_X02_A04_FIXES.md` for the code changes and full re-verification (103 tests + every existing audit script re-run clean). No current E-only richer sensor access found. Nulls, PCD wins and archive collapse are not blockers.

### Earlier E08 authorization gate (historical)

- Frozen definitions: metadata JSON serialization, feed/directory/response/packet schemas, canonical body bytes, dedup/FIFO and charged/excluded components.
- Fixtures: `tests/fixtures/accounting/README.md` plus A_feed, B_unique, C_duplicate, D_nondedup, E_fifo, F_oversize, G_shared_fifo, H_protocol and I_encoding JSON files.
- Author-side local checks: 9/9 PASS (not independent signoff on their own).
- **Independent acceptance was recorded at that checkpoint.** `audit/R04_ACCOUNTING_VERIFICATION.md` states that Sam rebuilt the canonical serializer, packet/feed/header schemas, and FIFO/dedup/byte-hours logic from the contract text alone and obtained 9/9 matches. The final pre-results audit reported that every pinned hash in that record differs from the current corresponding file — **this was correct.** The hashes were originally pinned (and Sam's first re-check also computed) against a local Windows working-tree read, which `core.autocrlf=true` silently converts from the repository's real LF content to CRLF. Re-verified against the canonical git-blob content (`git show HEAD:<path>`), all 11 hashes exactly match `PRE_RESULTS_AUDIT.md`'s own appendix; `git log --follow` confirms the amendment and all nine fixtures have had exactly one commit (`aaf0028`, the original freeze) touch them, ever — the content never changed, only the hashing method was wrong. Re-pinned to canonical values; see `audit/SAM_RESPONSE_TO_PRE_RESULTS_AUDIT.md`'s retraction note and `audit/R04_ACCOUNTING_VERIFICATION.md`.
- **E08 implementation was authorized and completed on that historical basis.** This does not waive the current accounting-custody reconciliation or the separate pre-results fixes before E12/X13. Aaron's optional second confirmation was not recorded as completed.

---

## 2. Decisions that have changed (don't assume the old answer still holds)

| # | Decision | Was | Now | Why it changed |
|---|---|---|---|---|
| 1 | Project | Candidate among 8–12 generated by the selection memo | **Evidence Before Erasure**, Track 2 | Scored highest (~82/100) on the selection rubric; least prior-art collision |
| 2 | Falsifiable hypothesis | Numeric threshold drifted to a descriptive estimand in the execution spec | **Resolved before outcomes:** PCD15 phase-average vs E30, held-out critical core, primary cap/interface/T, ≥10pp; all-phase request admissibility; no q promotion | `PRE_RESULTS_AUDIT.md` / `X13_RUN_CONTRACT.md` restore the unchanged practical threshold within a conditional simulation claim, not historical-loss inference. |
| 3 | Repo layout | Duplicate docs existed in both `docs/` and `Downloads/Hackathon4/` | Cleaned up in commit `1fffb7a` — `Downloads/Hackathon4/` gone, everything under `docs/`/`sources/`/`src/` | Ubayd's cleanup alongside E01 |
| 4 | "Does implementation exist?" | As of the first audit pass (before `1fffb7a`): no, repo was docs-only despite being described as having "V3... scoring, validation, pipelines" | As of `1fffb7a`: yes, real — `src/ebe/ingest.py`, independently re-run and byte-identical on a clean download | Ubayd shipped E01 |
| 5 | Novelty status | "Provisional, not certified absent" (searches blocked, no confirmed maintainer contact) | **Certified Novel** for Track 2 scope | Aaron & Jaswin completed formal prior-art audit (`sources/search_log.md`), differentiating EBE's collection policy benchmark from Boyd Kane's forensic tracing talk, collusion.wiki disclosures, and arXiv agent taxonomy literature. |
| 6 | Headline "14m22s propagation" example | Cited at face value in `DATA_AUDIT`/`EXECUTION_SPEC` as the propagation gap between `OAIEquityDec30Raw@5`→`OECDJun26PrecisionScout@14` | Jaswin's V02 pass read the full bodies and found the actual method/replication content appears earlier (`@4`, `@12`/`@13`); the true interval is 17–20 minutes, anchored on different revisions | Closer content reading during independent reconstruction — recorded as departure #7 in `docs/R04_FROZEN_SPEC.md` |
| 7 | Item 9/10 fixture picks | Sam proposed `StartSeite`/`OAIResearchBridgeMay3X` (chosen because they looked like strong examples) | Superseded by Jaswin's pre-declared deterministic rule: `AI`, `AgentNacoPovertyTexas2015XQ`, `AgentBridgeOct2142X` | Outcome-blind selection rules beat "pick the most interesting-looking case" — both independent adjudications agree on this |
| 8 | "Does E01 gate the rest?" | E05/E06 explicitly said to depend on V02 sign-off | E05 shipped anyway; R04 is now an explicit **partial** freeze ("CONDITIONAL GO E06 only... R04 PARTIALLY BLOCKED ON A03") rather than a full freeze | Team chose to proceed on the 6/10 titles with full agreement rather than wait for all 10 |

---

## 3. Are we getting there? — ticket tracker

**2026-09-13 E12 update:** The core-evidence evaluator candidate is implemented in `src/ebe/evaluator.py` and `src/ebe/a11_loader.py`, with its contract in `docs/E12_EVALUATOR.md`. It consumes all four frozen A11 artifacts, validates exact occurrence support and A05 split/group/eligibility invariants, builds body-hash-pinned occurrence alternatives, supports dev/held-out/full and critical-only evaluation, and computes three-state delay from retained snapshots only. The draft context artifacts remain disconnected; context reports `NOT_FROZEN`/NA. This implementation has not joined A11 to any real collector result and has not run a policy comparison or X13. X13 remains NOT AUTHORIZED.

Ticket IDs match `EXECUTION_SPEC_v0.1.md` §7. This is the real gauge — check this section, not your memory of the plan.

| Ticket | Owner | What it is | Status | Evidence |
|---|---|---|---|---|
| R00 | Jaswin + Aaron | Pin sources, rights, novelty search, maintainer email | ✅ **Done** | Hashes/sources independently verified by Aaron (`audit/reconstruction_checks.csv`). Novelty search log completed and certified (`sources/search_log.md`). Data rights resolved to clean-room download recipe (`sources/registry.json`). Maintainer questions prepared (`sources/maintainer_questions.md`). |
| E01 | Ubayd | Raw export inventory, no timeline assumptions | ✅ **Done** | `sources/{registry.json,SHA256SUMS,E01_ANOMALIES.md,schema_inventory.json}`, `src/ebe/ingest.py`. Independently re-run on a separately-downloaded copy of the export — byte-identical. |
| V02 | Sam + Jaswin | Manual state reconstruction/adjudication | ✅ **State-model comparisons accepted through V07, including a fresh blind re-derivation** | `audit/V07_BLIND_VALIDATION.md` reports revealed packets 04/08 matched. A second, structurally independent blind reconstruction (a freshly-spawned agent with zero access to any prior document, given only the raw export + `E06_IMPLEMENTATION_CONTRACT.md`) was obtained afterward — `audit/V07_SECOND_BLIND_RECONSTRUCTION.md` — and matched every boundary exactly, including independently re-finding the `OECDJun26PrecisionScout` touching-uncertainty-window case. |
| A03 | Alex + Aaron | Rubric pilot: atomic propositions, spans/hashes, support bundles, second review | ✅ **Pilot signoff recorded** | Ten-proposition pilot and Aaron/Jaswin review fields/adjudication are present. Reported primary span/hash checks do not certify whole-DSE context occurrences or A11; pre-results audit identified checker/schema and epistemic-prose issues to resolve before E12. |
| R04 | Jaswin, Sam review | Freeze experiment contract and accounting | ✅ **Design/run supplement frozen; accounting custody verified; A11 acceptance still separate/open** | `docs/X13_RUN_CONTRACT.md` fixes primary, admissibility, matrix, statistics and outputs. Accounting acceptance arithmetic passes 9/9; the reported 11-hash mismatch was real (Sam's original hashes were computed against a local CRLF working tree, not the canonical git blob) and is now fixed by re-pinning to canonical values, which exactly match `PRE_RESULTS_AUDIT.md`'s own appendix (`audit/SAM_RESPONSE_TO_PRE_RESULTS_AUDIT.md`, `audit/R04_ACCOUNTING_VERIFICATION.md`). `git log` still shows zero content edits since the freeze commit. A11 is now the separate, fully delivered benchmark freeze (see A11 row). |
| E05 | Ubayd | Typed validated loader | ✅ **Done** | `src/ebe/schema.py` + extended `ingest.py`, `docs/E05_TYPED_LOADER.md`, `tests/test_ingest.py`. Correctly types `revert` as body-unknown, never restoration. One gap from the E01 review still open: manifest `never_add_to`/`population_id` constraints are loaded as an opaque field, not enforced anywhere. |
| E06 | Ubayd | Timeline/state-at-time engine | ✅ **Implemented; state dependency sufficient for next ticket** | `src/ebe/timeline.py`, `docs/E06_TIMELINE_ENGINE.md`; V07 records no found state divergences with disclosed limitations. |
| V07 | Sam | Independent state reference/checks | ✅ **FULL PASS** (revised from CONDITIONAL) | `audit/V07_BLIND_VALIDATION.md` §6, `audit/independent_replay.py`, `audit/reconstruction_checks.csv`, `audit/V07_SECOND_BLIND_RECONSTRUCTION.md`. The two gaps that held this at CONDITIONAL are both closed: the `data/data/raw/export` path bug is fixed (`b1c822e`), and a second, zero-prior-exposure blind reconstruction now confirms every boundary. Does not verify accounting bytes — that's the separate, still-open E08 accounting-fixture acceptance below. |
| E08 | Ubayd (fixes by Sam) | Shared observer/storage | 🟡 **Implemented; narrow neutrality retained; X01/X02 and snapshot-integrity repairs still required (closure §§2–4). Prior fix report below is historical.** | `src/ebe/{accounting,observer,storage}.py`, tests and `docs/E08_OBSERVER_STORAGE.md`. The pre-results audit's checkpoint-response-accounting (X01), causal-ordering (X02), and retained-only-boundary (A04) findings are now fixed in `src/ebe/observer.py`/`collectors.py` and independently re-verified (`audit/X01_X02_A04_FIXES.md`): full 103-test suite plus every existing audit script re-run clean. |
| E09–E10 | Ubayd | Periodic/event-derived collectors | ✅ **Implemented; independently cross-policy audited PASS** | `src/ebe/collectors.py` implements P/PD/PCD and bounded `E(q)`. `audit/E10_CROSS_POLICY_AUDIT.md` and `audit/verify_collectors_cross_policy.py` record Sam's independent PASS: sensor/access and accounting parity, q=30/100/300 token-bucket re-derivation, nine adversarial scenarios, and no collector-code bug found. A01 reconciled. F (comparator collector) remains unimplemented and required before X13. No semantic results. |
| A11 | Alex/Aaron/Jaswin (fixes by Sam) | Freeze benchmark evidence, blind to policy outputs | 🟡 **Development-useful, NOT final-scoring ready; see closure §6. Legacy 14/14 does not certify semantic/schema/encoding/split acceptance. Prior delivery report follows.** | Full benchmark (`annotations/A11_BENCHMARK_SPEC.md`): 65 propositions (**57 critical, 8 controls** — corrected 2026-09-13; the "49/16" figure previously reported here and in the spec's own executive summary/§4 table did not match `evidence.jsonl`'s actual `critical` field or `splits.json`'s own `summary` block, which have always said 57/8), 71 episodes (37 dev / 34 held-out), 25 copy groups, 1,361 occurrences (717 dev / 644 held, not the previously-reported 714/647), 50 pages (revised from 43). Sam's from-scratch re-verification (`audit/A11_INDEPENDENT_VERIFICATION.md`, `audit/verify_a11_independent.py`) found and fixed: (1) 7 cross-title pages / 9 occurrences missing from `splits.json`'s group/page catalog (added, with provenance notes), (2) epistemic-leakage prose in `PROP-20260620-09`/`PROP-20260619-03` (revised; `evidence.jsonl` hash re-pinned everywhere), and (3) the spec's own summary/§4-table counts were stale versus the real per-split data (corrected in place, no annotation content changed). The document's earlier "Independent Audit: Sam" / "E12+X13 fully unblocked" claims were false and are corrected in place. **A far larger, independently-produced "A11 acceptance matrix" (items A–J) identifies further gaps; items C/D/E's mechanically-closeable portions have since been done (see `audit/A11_ITEMS_CDE_PROGRESS.md`): occurrence census now 1,421 (was 1,361, +60 exact-match rows for a proposition that was missing even its own anchor occurrences); `annotations/eligibility.jsonl` added (64 eligible, 1 excluded per an already-adjudicated head-mismatch exclusion in `docs/V02_ADJUDICATION.md`); `src/ebe/a11_loader.py` mechanically converts real `evidence.jsonl` spans into the evaluator's Fragment/Proposition types. **Sam has since produced an independent DRAFT of the context axis** (`docs/A11_CONTEXT_AXIS_DRAFT_SAM.md`, `annotations/context_fragments_DRAFT_SAM.jsonl`, `annotations/context_eligibility_DRAFT_SAM.jsonl`) — explicitly a comparison artifact, not accepted ground truth, to be reconciled against Alex/Aaron/Jaswin's own version. It found a real discrepancy against the acceptance matrix's own inventory (2 additional propositions, `-04`/`-05`, needing cross-revision context the matrix's inventory omitted) and one near-copy occurrence candidate (`PROP-20260616-10`, recommended against inclusion as likely-generic boilerplate, not confirmed). Items B/F/G/J are untouched. No real evidence-coverage scoring was run against any of this, draft or otherwise. |
| E12 | Ubayd, Sam review | Evidence-coverage/delay evaluator | 🟡 **CONDITIONAL GO engineering; partial E12 requiring snapshot, population/split, copies, encoding, eligibility and context repairs—not context alone (closure §§8–9)** | `src/ebe/evaluator.py` (12/12 hand-scored synthetic tests) + `src/ebe/a11_loader.py` (5/5 structural tests, `tests/test_a11_loader.py`) — see `docs/E12_SUBSTRATE.md` and `audit/A11_ITEMS_CDE_PROGRESS.md`. Implements the SS4 retained-only firewall (poison-tested against real collector output), SS5 micro coverage/delay core, A05 denominator firewall, and now a real loader converting `evidence.jsonl`'s spans + `annotations/eligibility.jsonl` into the evaluator's types. Deliberately does NOT implement F/PCD-R/archive/phase-robustness. Does NOT run coverage/delay against any real collector output — only structural loading/firewall checks. Real scoring remains blocked specifically on the **context axis** for real (accepted) ground truth: `src/ebe/a11_loader_context_draft.py` + `tests/test_a11_loader_context_draft.py` (4/4 structural tests) wire in Sam's DRAFT context data (`docs/A11_CONTEXT_AXIS_DRAFT_SAM.md`) for comparison purposes only — still no coverage/delay scoring run against any collector output. Context fragments/eligibility/near-copy review await Alex/Aaron/Jaswin's own version to reconcile against. |
| X13 | Ubayd + Sam | Pinned sweep, clean-environment reproduction | ⬜ **Not run; NOT AUTHORIZED** | Run contract frozen, execution NOT authorized. X01/X02 only partially repaired; F/X03, synchronized cost/overhead/reproduction X04, independent finalized E12 acceptance X05, A11 acceptance and S01–S05 machinery remain open. See `PRE_RESULTS_CLOSURE.md`. |
| P14 | Jaswin + Alex | Judge-facing artifact (figure + one history panel + README) | ⬜ Not started |

**Plain read (current):** E08/E09/E10 scheduling implementation remains narrowly fair, but X01/X02/A04 full closure was overstated. A11 and E12 are incomplete beyond the context axis. **E12 CONDITIONAL GO for outcome-blind repairs under `PRE_RESULTS_CLOSURE.md` §9; X13 NOT AUTHORIZED.** Ticket evidence narratives above include historical implementation claims, not replacement acceptance. All null, reversal and unmatched outcomes remain publishable.

---

## 4. Earlier verification summary (historical)

Preserved from earlier checkpoints. In particular, timing arithmetic is not first semantic propagation, archival-clock correlation is not visibility, and prior novelty prose is not a methodological gate closure. Current audit findings are in section 1 and `PRE_RESULTS_AUDIT.md`.

| Claim | Verified how | Status |
|---|---|---|
| Export hashes/row counts match what `DATA_AUDIT` recorded | Re-downloaded independently, re-hashed, re-ran `ingest.py` | ✅ Confirmed, twice, from two separate downloads |
| E01's specific edge-case numbers (5,217 deletions, 1,246 deletion-only titles, 67 rows/68 edges, 4 bodyless `revert`s) | Recomputed from raw JSONL independently of `ingest.py` | ✅ Confirmed |
| DATA_AUDIT's quoted timing examples (96s notice→backup, 12s original→backup deletion, 14m22s cross-page propagation) | Recomputed from raw timestamps, not copied | ✅ Confirmed, exact to the second, on 3 separate examples |
| `archived_at` reliably reflects "next mutation time" | Tested programmatically across 50 consecutive pairs | 🟡 Mostly (48/50 within ±1s), but **two real multi-minute exceptions found** — flagged as a live risk, not yet resolved |
| Novelty (no one else has done this exact experiment) | Search engines blocked/unreliable per `DATA_AUDIT`; no confirmed maintainer reply | 🔴 Still unverified |
| Four-hour gate passed | EXECUTION_SPEC's own criteria (10 cases reconstructed & agreed, no future-info leakage, etc.) | 🟡 Substantially satisfied by informal work, **but never formally declared passed by the team** |

---

## 5. Earlier open-decision register (historical)

This list predates V07 and the accounting amendment; it is preserved as historical context, not current authorization. V07 discloses and accepts state-model checks despite prior exposure; A03 pilot signoff is now recorded. The **current** gate register is `PRE_RESULTS_AUDIT.md` §11 and section1 above, including audit/hash custody, A11 and retained-evidence boundaries. The hypothesis decision below is resolved by the pre-results run contract; source/annotation/release limitations remain separate.

1. **The blind-fixture plan is compromised.** Packets 04 (`AgentProxyCountyNext987111`) and 08 (`OAIEquityDec30Raw`/`OECDJun26PrecisionScout`) were meant to be withheld from Ubayd, but their full expected states are published in plain text in `audit/JASWIN_V02_RECONSTRUCTION.md`, which is committed to the same repo Ubayd works in. `docs/E06_IMPLEMENTATION_CONTRACT.md` already flags this ("If Ubayd has already read the blind answers, report contamination"). **Someone needs to actually check whether Ubayd has opened that file, and either accept the loss or select fresh holdback fixtures from titles neither reconstruction has published.**
2. **Sam's independent reconstruction of packets 09/10 is now contaminated.** Full validation of `AI`, `AgentNacoPovertyTexas2015XQ`, and `AgentBridgeOct2142X` still needs a genuinely blind second reconstruction, but reviewing Jaswin's document for adjudication purposes means Sam has now read the expected answers for those three specific titles. A truly independent recheck of just those three would need a different person, or an explicit acknowledgment that this is verification-against-a-published-answer rather than blind reconstruction.
3. **A03 real pilot still doesn't exist.** Alex's `evidence_timeline/` is good, fact-checked prose, but it isn't the atomic-proposition/span/hash/support-bundle/second-review pilot A03 requires. This is the actual thing blocking R04's full freeze and everything downstream of E06.
4. **Maintainer email still not sent.** It's fully drafted (`sources/maintainer_questions.md`) — send it now regardless of reply odds.
5. **`never_add_to` guard** — flagged after E01, still not implemented after E05. Needs to land before E12, or it becomes a much more expensive fix later.
6. **Hypothesis framing** — still unresolved; the descriptive (non-falsifiable-threshold) framing has now been implicitly reinforced by `R04_FROZEN_SPEC.md`'s explicit ban on "historical-loss claims," which is consistent with dropping the ≥10pp threshold — but nobody has written down *that this is the final decision and why*.

---

## 6. Earlier time reality check (historical, not a current estimate)

The following planning assessment predates E06/V07 and is not used to authorize implementation.

Deadline: 2026-09-13 23:59 AoE. As of this update, roughly **one day** of runway remains, not the "48 hours" the original schedule in `EXECUTION_SPEC_v0.1.md` §16 assumed (that schedule was written assuming a start point that, based on repo evidence, didn't actually happen until partway through day one). With 1 of 14 tickets done:

- If the team wants the **ideal** submission (160 episodes, full sweep, P14 judge artifact) — not realistic on remaining time at current velocity.
- The spec's own **minimum viable submission** (§16: one wiki population, 40–60 adjudicated episodes, 3 policies, 2 intervals, one retention figure, reproducible tables, honest limits) is still reachable **only if E05 unblocks in the next few hours.**
- The spec's own kill/pivot ladder (§17) is the honest fallback if it doesn't: narrow scope, declare it a trace-grounded simulation rather than historical reconstruction, or pivot to Witness Budget. None of those conditions have been triggered yet — this is a "watch the clock," not a "pivot now," situation.

---

## 7. Change log (append here as things move)

- **Closure review, baseline `9a7ab4c`:** created `docs/PRE_RESULTS_CLOSURE.md`, independently reviewing every original audit item and subsequent code/tests without X13 or real semantic scoring. **E12 CONDITIONAL GO; X13 NOT AUTHORIZED.** Verified narrow collector fairness and11/11 accounting pins; reproduced124 passing tests. Reopened overclaimed A03/A04/A05 and X01/X02 closure: ignored15 existing cross-title occurrences, held-out pilot contamination, source-hash domain mismatches (2 evidence/80 occurrences), incomplete occurrence eligibility/review/sampling, unchecked retained snapshots and residual causal API defects. Context draft omits core requirements and cannot enter headlines. Exact next owners: Ubayd engineering; annotation team semantic freeze; Sam independent acceptance. Historical entries below are preserved, including statements this review supersedes.

- **2026-09-13, Sam produces an independent DRAFT of the A11 context axis (for comparison, not accepted ground truth):** `docs/A11_CONTEXT_AXIS_DRAFT_SAM.md`, `annotations/context_fragments_DRAFT_SAM.jsonl`, `annotations/context_eligibility_DRAFT_SAM.jsonl`, `audit/near_copy_search_DRAFT_SAM.py`, `src/ebe/a11_loader_context_draft.py`, `tests/test_a11_loader_context_draft.py`. Explicitly produced to be reconciled against whatever Alex/Aaron/Jaswin independently produce for the same requirement — every artifact name says `DRAFT_SAM`, and none of the real, already-accepted `evidence.jsonl`/`occurrences.jsonl`/`eligibility.jsonl` files were touched. Derived context requirements directly from `evidence.jsonl`'s own `support_bundles.context_complete` field (already-authored data, not new invention): 59/65 propositions need only their own anchor save (modeled as an observable-feed fragment); 6 need a genuinely different revision/event, each independently verified as a real exact substring/event match before being recorded. **Found a real discrepancy against the independently-produced acceptance matrix's own inventory**: it named only 4 propositions (`-03/-07/-08/-09`) needing additional context revisions, but direct re-derivation finds 2 more (`-04`, `-05`), each needing a cross-page body span *and* a delete event — including a specific, worth-double-checking read that `-05`'s required delete is of the ZZZ *backup* page itself, not the original. Also ran a normalized (whitespace/case-insensitive) near-copy search one step past the exact-match census: found `PROP-20260616-10` (a non-critical negative control) has near-matches on 5 other pages, recommended (not confirmed) against inclusion since the matched text is a generic reused API-documentation URL, consistent with the proposition's own "ordinary control" classification. Genuine semantic/paraphrase detection was not attempted. No coverage/delay scoring was run against this draft data. Full suite: 124/124.

- **2026-09-13, Sam closes the mechanically-closeable halves of acceptance-matrix items C/D/E:** `audit/A11_ITEMS_CDE_PROGRESS.md`, `audit/occurrence_census_search.py`, `annotations/eligibility.jsonl`, `src/ebe/a11_loader.py`, `tests/test_a11_loader.py`. Explicitly scoped to reshaping/searching/rule-applying against already-authored or already-adjudicated data, never inventing new semantic judgment: (D) an exhaustive whole-DSE exact-match search found `PROP-20260619-01` missing 60 occurrence rows including its own anchor's already-recorded spans — fixed, and a latent multi-span comparison bug in both annotation checkers (comparing an occurrence's slice against the full ellipsis-joined `quotation` instead of its own individual `source_span`) found and fixed along the way; occurrence total 1,361 → 1,421. (E) `annotations/eligibility.jsonl` marks 64/65 propositions core-eligible; the one exclusion (`PROP-20260618-63`, anchored on the documented `dse~AI` head-mismatch title) cites an exclusion already adjudicated in `docs/V02_ADJUDICATION.md`, not a new call; all 65 anchors independently reconfirmed as known, unambiguous `LIVE_BODY` states via the real `TraceModel`. (C) `src/ebe/a11_loader.py` mechanically reshapes `evidence.jsonl`'s existing `source_spans` into the evaluator's Fragment/Proposition types. **Context fragments/eligibility and near-copy occurrence review remain open** — genuinely require Alex/Aaron/Jaswin's judgment, not attempted here. No real evidence-coverage scoring was run. Full suite: 120/120; all 6 audit scripts clean.

- **2026-09-13, Sam builds the E12 substrate (not full E12):** `src/ebe/evaluator.py`, `tests/test_evaluator.py`, `docs/E12_SUBSTRATE.md`. Implements only what `docs/X13_RUN_CONTRACT.md` §9 authorizes before A03/A11's fragment/eligibility schema exists: the §4 retained-only evaluator firewall (`RetainedSnapshot`, built exclusively from `CollectorResult.retained_evidence`/`.feed_polls`, never `body_results`/`capture_attempts`) and the §5 micro coverage/delay core, against a synthetic `Fragment`/`Proposition` representation explicitly documented as scaffolding, not a proposal for the real annotation schema. 12/12 hand-scored synthetic acceptance tests pass (duplicate-support, cumulative-body, context-independent-of-core, encoding consistency, NA-eligibility, empty-denominator, both population-firewall rejections, all three delay statuses), including one that drives the real `Observer`/`PeriodicCollector` at `capacity_bytes=0` to prove the firewall holds against genuine collector output. Full project test suite: 115/115 (up from 103). Deliberately does not implement F/PCD-R/archive/phase-robustness or read any real annotation file — real scoring stays blocked on acceptance-matrix items C/D/E, which remain Alex/Aaron/Jaswin's work.

- **2026-09-13, Sam retracts and fixes the A02 correction, plus an independently-produced A11 acceptance matrix identified real count/custody gaps:** An external "A11 acceptance matrix" (items A–J) checked this project's A11 delivery against `docs/X13_RUN_CONTRACT.md` §4's requirements and found several real, mostly annotation-authorship-level gaps (pilot-isolation reconciliation, typed fragment alternatives, eligibility masks, encoding provenance, occurrence-census completeness, adjudication depth, sampling/episode-graph documentation) that belong to Alex/Aaron/Jaswin, not to mechanical fixing — these remain open and are not resolved in this entry. Two items in that matrix WERE mechanical and are fixed here:
  - **Item A (counts):** `annotations/A11_BENCHMARK_SPEC.md`'s executive summary and §4 split table had stale numbers (49 critical/16 non-critical total; 35/30 dev-held propositions; 714/647 occurrences) that never matched `evidence.jsonl`'s own `critical` field or `splits.json`'s own `summary` block (both of which have always said 57 critical/8 non-critical; the correct per-split breakdown is 34/31 propositions, 30/27 critical, 717/644 occurrences). No annotation content or judgment changed — corrected the doc's transcription and added a permanent regression check to `audit/verify_a11_independent.py` (§1b).
  - **Item H (custody/hash domain), and independently discovered to affect A02 too:** all SHA-256 hashes this reviewer had pinned anywhere in this project (11 R04 accounting fixtures, `evidence.jsonl`, `occurrences.jsonl`) were computed against a local Windows working-tree read (`sha256sum`/`open(path,'rb')`), and this machine has `core.autocrlf=true`, which silently converts the repository's real LF-stored content to CRLF on checkout. **This means Sam's earlier "A02 does not reproduce" correction (in `audit/SAM_RESPONSE_TO_PRE_RESULTS_AUDIT.md`) was itself wrong — Jaswin's pre-results audit was right.** Re-verified using `git show HEAD:<path> | sha256sum` (the canonical, checkout-independent content): all 11 R04 fixture hashes now exactly match `docs/PRE_RESULTS_AUDIT.md`'s own appendix table. No file content was ever wrong — only the byte representation used to hash it. Fixed by: re-pinning `audit/R04_ACCOUNTING_VERIFICATION.md`, `annotations/splits.json`, and `annotations/A11_BENCHMARK_SPEC.md` to canonical values; adding a root `.gitattributes` (`* text=auto eol=lf`) and running `git add --renormalize .` (which changed zero tracked file bytes — every commit had, in fact, always stored LF correctly; only local reads were wrong) so this class of bug can't recur silently; and pinning two artifacts (`splits.json` itself referenced externally, `adjudication.csv`) that previously had no hash at all ("Recorded in repository").
  - **Not addressed in this entry, and not something to silently fabricate:** items B (pilot-isolation reconciliation — decision belongs to Jaswin), C (typed fragment/context alternatives schema), D (whole-DSE occurrence census completeness search), E (machine-readable eligibility mask), F (encoding-provenance/source-hash reconstruction for two specific propositions), G (adjudication depth beyond claim_status), J (sampling/episode-graph provenance documentation). These require Alex/Aaron/Jaswin's substantive annotation authorship. Given the deadline (today, 2026-09-13, 23:59 AoE), the team should decide explicitly whether to address any of these or document them as known limitations in the final submission — that decision itself needs a human, not an automated fix.

- **2026-09-12, Sam fixes X01/X02/A04 and the two remaining A11 gaps (given explicit authorization to implement, not just report, under deadline pressure):** Supersedes the "not fixed"/"implementation work for Ubayd" language in the two entries directly below — those describe the bugs as originally confirmed, the fixes came after.
  - **X01 (checkpoint response accounting):** `Observer.costs(checkpoint)` now recomputes outcome/pending/downloaded-byte fields from the per-request audit trail as-of-checkpoint (any response with `response_time > checkpoint` is now correctly pending, contributing zero bytes/outcome), instead of trusting eager counters that only ever compared against `HORIZON_END`. `CollectorResult.outcome_counts` fixed the same way.
  - **X02 (causal ordering):** `get_body` now rejects a `request_time` earlier than the most recent `poll_feed` call; `poll_feed` now rejects being called after `terminal_directory` (two-directional mutual exclusion).
  - **A04 (retained-only boundary):** added `RetainedEvidenceUnit`/`CollectorResult.retained_evidence`, built exclusively from `CaptureStore.retained_captures` + `body_for_capture` (which structurally cannot return an evicted/oversize body). `body_results`/`capture_attempts` are unchanged (still needed as diagnostic history) but now explicitly documented as not-for-scoring.
  - Full re-verification: 103-test suite passes; `verify_collectors_cross_policy.py`, `verify_e08_neutrality.py`, `verify_ahi_against_real_code.py`, `verify_accounting.py`, and both smoke scripts all re-run clean. Two of Sam's own `verify_e08_neutrality.py` test cases had to be rewritten (they relied on the exact ordering X02 now forbids — dispatching a GET timestamped before a poll that had already run). Full detail: `audit/X01_X02_A04_FIXES.md`.
  - **A11's two remaining gaps also fixed:** 7 cross-title pages (not 5 — a second full pass caught 2 more the first truncated failure message missed) added to `splits.json`'s group/page catalogs with inline provenance notes; `PROP-20260620-09`/`PROP-20260619-03`'s `critical_reason`/`notes` revised to stop treating an agent self-report as externally confirmed fact. `evidence.jsonl`'s hash changed and was re-pinned in `splits.json` and `A11_BENCHMARK_SPEC.md`. `audit/verify_a11_independent.py` now reports 14/14. Full detail: `audit/A11_INDEPENDENT_VERIFICATION.md`.
  - **Net effect:** A01–A04 closed. E12 implementation may begin (A05 is a constraint on E12's own implementation, not a precondition). X13 remains NOT AUTHORIZED — F/X03 unimplemented, X04/X05/S01–S05 untouched.

- **2026-09-12, Sam's independent A11 re-verification:** `audit/A11_INDEPENDENT_VERIFICATION.md` + `audit/verify_a11_independent.py` (written from scratch, not importing `audit/verify_annotations.py`). Corrected `annotations/A11_BENCHMARK_SPEC.md`'s false "Independent Audit: Sam" attribution and false "E12/X13 fully unblocked" claim in place. Confirmed real: all 65 propositions' body hashes and character spans, all 1,361 occurrences' hashes/spans, adjudication completeness (65/65), and the two declared splits' mutual disjointness. Found and reported (not fixed — `splits.json` is Alex's frozen deliverable): 5 cross-title mirror pages (9 occurrences, under GRP-03/GRP-04/GRP-21) referenced in `occurrences.jsonl` but absent from `splits.json`'s `groups`/`page_keys` catalog entirely — no cross-split contamination, just a completeness gap that could silently drop those occurrences from a naive evaluator's scoring. Also re-checked and confirmed **still present, unrevised**: the specific epistemic-leakage prose (`PROP-20260620-09`, `PROP-20260619-03`) the pre-results audit named against the original 10-proposition pilot. Also fixed the objective tooling bug in `audit/verify_annotations.py` (wrong doubled `data/data/raw/export` path, no failure exit code) that the pre-results audit's D04 finding named.

- **2026-09-12, Sam's independent response to the pre-results audit:** `audit/SAM_RESPONSE_TO_PRE_RESULTS_AUDIT.md`. Independently re-derived, with fresh reproductions distinct from the audit's own snippets, and **confirmed real**: X01 (checkpoint response accounting counts a post-checkpoint completion as downloaded/successful even though storage correctly excludes it — root-caused to `Observer.get_body`/`costs()` only checking `HORIZON_END`, never the collector's own earlier checkpoint), X02 (`get_body` has no ordering guard against the poll that actually discovered a page, allowing a GET timestamped before its own discovery to return a body — not exploited by any shipped collector), and A04 (`body_results`/`capture_attempts` expose full bodies even under a zero-byte cap where nothing is retained — confirmed via a real capacity_bytes=0 run). **Corrected**: A02 (accounting-hash mismatch) does not reproduce — recomputed all 11 SHA-256 hashes against the current clean working tree, all match `audit/R04_ACCOUNTING_VERIFICATION.md`'s pinned values exactly, and `git log --follow` shows the amendment/fixtures have had exactly one commit (the original freeze) touch them ever. **Closed**: D04's criticism of `audit/E08_NEUTRALITY_AUDIT.md` (its A/H/I row overstated chronology, citing a pre-implementation acceptance pass that used Sam's own simulator, not the real `accounting.py`, as if it were real-code coverage) is accepted and fixed for real via a new `audit/verify_ahi_against_real_code.py` — drives A/H/I's exact claims (feed/array framing, all six outcome-header byte counts, terminal-directory framing, ASCII/UTF-8/Latin-1 canonical-body equivalence, empty-body/generic-serializer/packet-width probes) through the actual shipped `Observer`/`accounting`/`storage` code, 0 failures. `E08_NEUTRALITY_AUDIT.md` item 8 corrected in place. No fixes applied to `src/ebe/observer.py` or `collectors.py` — X01/X02/A04 remain open, real, and are implementation work for Ubayd.

- **2026-09-12, A11 benchmark freeze (Alex + Aaron + Jaswin):** Delivered complete evaluation benchmark expanding from 10 pilot propositions to **65 propositions** across **71 page episodes** in **25 title groups** (13 dev / 12 held-out). Whole-DSE occurrence census across 13,403 revisions produced **1,361 occurrences**. Split isolation verified (zero overlap across dev and held-out splits). 100% character spans and SHA-256 body hashes verified by `audit/verify_annotations.py`. Adjudication signed off (`annotations/adjudication.csv`). Frozen specification documented in `annotations/A11_BENCHMARK_SPEC.md` with cryptographic checksums pinned in `annotations/splits.json`. E12 evaluator unblocked.
- **Final pre-results methodological audit, baseline `9342cda`:** created `docs/PRE_RESULTS_AUDIT.md` and `docs/X13_RUN_CONTRACT.md`; verdict **CONDITIONAL GO E12**, X13 **NOT AUTHORIZED**. Frozen PCD15/E30 primary, unchanged 10pp threshold, all-phase request admissibility, full matrix/statistics/outputs and mandatory archive/repair/order/chronology sensitivities without policy outcomes. Reproduced 103 tests and 9/9 accounting arithmetic; synthetic probes demonstrated checkpoint-accounting, causal API and unretained-body exposure issues. At that local baseline the E09/E10 independent report was absent; it has now arrived through this merge and must be reconciled under A01 rather than described as missing. The audit also found 11/11 accounting acceptance hash mismatches. A03 pilot is not A11 acceptance. No collector/scorer/annotation code changed; no coverage or experiment run.
- **2026-09-12, E09/E10 audit (arrived from remote in current merge):** Ubayd shipped E09 (`fdcd5b1`) and E10 (`9342cda`). `audit/verify_collectors_cross_policy.py` and `audit/E10_CROSS_POLICY_AUDIT.md` record Sam's independent cross-policy audit: sensor-access parity over the allowed Observer surface; an independently coded token-bucket simulator matching q=30/100/300 behavior including burst drain and body-unknown charging; nine adversarial scenarios PASS; and no artificial PCD weakening found at matched schedule granularity. The audit records zero bugs in `src/ebe/collectors.py` and five corrected bugs in its own harness. This collector-level PASS is retained, but it did not implement/run semantic scoring and does not authorize E12 or X13.

- **Pre-E08 accounting recovery checkpoint:** Preserved the existing amendment and A/B work; completed nine data-only accounting fixtures and README. Author-side direct arithmetic plus local standard-library serialization/UTF-8/hash checks passed all nine. No Sam/Aaron acceptance exists, so **E08 STILL BLOCKED**. Updated stale E06/V07 status from their local documents; no source semantics/policy parameters changed, no implementation code created, no commit/push.

- **2026-09-12, early:** Selection memo produced (`Astra_Project_Selection_Analysis.md`), Evidence Before Erasure chosen.
- **2026-09-12:** `DATA_AUDIT_2026-09-12.md` + `EXECUTION_SPEC_v0.1.md` produced — data-semantics audit and frozen v0.1 spec. Falsifiable hypothesis quietly dropped in this pass.
- **2026-09-12:** First scientific audit performed (repository found to be docs-only at that point, despite being described otherwise).
- **2026-09-12:** Independent validation pass — 7 of ~10 fixture cases reconstructed directly from raw export, cross-verified several DATA_AUDIT claims, found the `archived_at` lag pattern.
- **2026-09-12, commit `1fffb7a`:** E01 shipped by Ubayd. Independently re-verified — byte-identical clean-room reproduction. Repo cleaned up (duplicate docs removed).
- **2026-09-12, commit `0de69b0`:** E05 typed loader shipped by Ubayd (`schema.py` + extended `ingest.py` + tests). `never_add_to` gap from E01 review confirmed still open.
- **2026-09-12, commit `bf58327`:** Jaswin's independent V02 reconstruction (`audit/JASWIN_V02_RECONSTRUCTION.md`) + R04 pre-freeze decisions + maintainer email drafted (not sent).
- **2026-09-12, commits `e8fa731`/`519f045`:** Alex added `evidence_timeline/` — a sourced narrative CSV. Independently spot-checked here against raw JSONL, including the most dramatic claim (heartbeat-key dead-man switch, `hb001`–`hb353`) — holds up factually, but does not satisfy A03's pilot criteria.
- **2026-09-12, commit `13bb0c5`:** Jaswin froze `R04_FROZEN_SPEC.md` (partial — CONDITIONAL GO E06 only, blocked on A03), an independent `docs/V02_ADJUDICATION.md`, and `E06_IMPLEMENTATION_CONTRACT.md`. Independently confirmed: zero disagreement between the two V02 reconstructions on any shared, non-blind title. Two process problems surfaced: blind fixtures 04/08 are exposed in-repo, and packets 09/10 now need a genuinely fresh independent reconstruction since Sam has read Jaswin's answers during adjudication.
- **2026-09-12:** Alex delivered the real A03 pilot package (`annotations/rubric.md`, `annotations/evidence.jsonl`, `annotations/occurrences.jsonl`, `annotations/A03_PILOT_HANDOFF.md`) satisfying all six criteria: 5 atomic propositions with exact Unicode spans [start, end), SHA-256 hashes, support bundles, and 102 verified occurrences across Case 01/02. Handed off to Aaron for second review.
