# Final pre-X13 consolidation — FINAL_PRE_X13_v1

**Disposition: READY FOR FINAL ENGINEERING PASS. X13 NOT RUN / NOT YET AUTHORIZED.**

Research-methodology owner: final outcome-blind consolidation under the user's explicit mandate. Baseline reviewed: `ade1f4c`. This document is the final methodological authority with `X13_RUN_CONTRACT.md` amendment `FINAL_PRE_X13_DEADLINE_v1`. It supersedes older broad gate registers and stale status headings; unamended R04 accounting and trace rules remain binding. It is not a claim that Sam independently accepted this new candidate.

**Outcome-access declaration:** no real PCD-vs-E result exists in this review; no X13 execution, real collector/annotation join, or policy coverage inspection occurred. Source bodies were read solely for adjudication; structural annotation checks and a loader-only inventory check were run. No parameters, labels or counts were selected using collector outcomes. No embedded URL was opened or executed.

## 1. Final A11 disposition

**A11 CONTEXT FROZEN. A11 semantic benchmark frozen.** No further semantic redesign is requested. Engineering must compile and validate these exact annotations, not decide their meaning. Independent Gate 2 checks conformance and may identify a real contradiction; it does not reopen unrelated judgments merely because results might be inconvenient.

### PROP-20260616-61: option 2 — multiple valid alternative context fragments

Read the complete raw source bodies of all 37 matches found by whole-export search for the literal ordered triple and its one/two-level percent encodings (three decoding passes used for candidate discovery only). Four pages match: three helper pages (17 revisions) and the anchor page (20 revisions). The complete three June 1 sources are independently corroborated in Sam's context check and Alex's `A11_PROP61_GROUNDABILITY_REVERIFICATION.md`.

| Alternative | Frozen initial source | Selected save | Canonical span, half-open |
|---|---|---|---|
| CTX-PROP-20260616-61-01 | `dse~AgentIvyTuitionValues2015XQ@1` | June 1 16:14:15Z | [56,248) |
| CTX-PROP-20260616-61-02 | `dse~AgentDataUsaUnique5@1` | June 1 16:03:04Z | [19,242) |
| CTX-PROP-20260616-61-03 | `dse~AgentIvyLink@1` | June 1 15:54:26Z | [26,265) |

Each span is a tuition-query URL encoding `University:107327,199333,198321` in that order. All precede the June 16 19:47:51Z anchor. The first is a direct API URL; the others are reader-wrapper URLs. Differences in year restriction, locale and URL encoding do not change the referenced ordered tuition-query pattern. This is specific task context, not PROP-10's generic endpoint boilerplate. The referent is a class of older helper pages, not a uniquely named document: **any one is sufficient interpretive context**, not all three jointly required. Multiple candidates therefore do not make the context unavailable or ambiguous for this scoped requirement.

The retained context does **not** prove which page the author meant/read, that a query executed, distinct agent identities, or that Cleveland really was the next task. Core remains the observed publication of a prediction with its stated rationale. These epistemic boundaries resolve the apparent corroboration-versus-interpretation tension without changing the core label. The anchor's twenty repetitions do not independently supply the external helper pattern. The different-order six-ID superset discussed in Sam's report is not this ordered query pattern and is not admitted as context.

`AgentIvyTuitionValues2015XQ` has fifteen matching revisions (@1–3 and @5–16); later versions preserve the exact query while appending/changing unrelated research material. They are exact carryforward context occurrences, not fifteen new alternatives or earlier acquisitions. The two other helper titles have one occurrence each. Their inclusion adds two context-only pages to existing held-out GRP-19; no group/proposition moves, core occurrence additions or new episodes. The factual claim that no matching source exists is **withdrawn**, including in the accepted reconciliation report. Original independent reports are preserved as dated review history.

The other seven context judgments are retained. Direct mechanical gaps are closed by pinning the two deletion fragments to exact observable predicates: `(dse~AgentLinkma21JuneAA, delete, 2026-06-18T18:26:23.000000Z, multiplicity>=1)` and `(dse~AgentProxyCountyNext987111, delete, 2026-06-18T18:23:13.000000Z, multiplicity>=1)`. Raw event IDs are audit references only. No private actor/event ID becomes feed evidence.

### Final inventory (nominal July 15)

| Quantity | Development | Held-out | Total / interpretation |
|---|---:|---:|---|
| Propositions | 38 | 27 | 65 |
| Critical propositions | 34 | 23 | 57 |
| Noncritical propositions | 4 | 4 | 8 |
| Core-eligible propositions | 38 | 27 | 65 |
| Primary eligible critical denominator K | — | **23** | common to every primary row |
| Groups | 14 | 11 | 25 existing purposive groups |
| Catalog pages including context | 30 | 22 | 52; previously 50 before two helper dependencies |
| Core occurrence rows | 808 | 613 | 1,421 span records, not propositions |
| Context-needed propositions | 6 | 2 | 8 |
| Context-groundable propositions | 6 | 2 | 8 |
| Self-contained propositions | 32 | 25 | 57 |
| Declared episode IDs | 39 | 32 | 71 catalog entries ONLY |

Context definitions: **10** (eight body, two event); exact context body occurrence rows: **101** (84 for the five prior body definitions plus 17 Ivy matches). Zero unknown/unavailable required-context propositions in this frozen inventory; the interface must still support NA for such states. Zero unresolved core labels or whole-proposition core exclusions. **One excluded support revision/one core occurrence row:** `PROP-20260618-63` / `dse~AI@2`; the eligible `dse~AgentSecCountyVarAI@1` alternative remains. All 65 have computable nominal earliest exact support in the current structural loader; this is not capture/delay scoring. The loader contains 1,420 compiled core span fragments after that exclusion. K's one-unit granularity is `100/23` pp; do not round for decisions.

Unknown prehistory, BU, untimed head-mismatch occurrences, incompatible states and unheld history remain subject to the frozen source/eligibility rules. Zero annotated unresolved units is **not** evidence of complete historical coverage. The derived conservative mask can exclude additional support/units; its algorithm is frozen here, counts must be generated before scoring, never guessed from nominal counts.

**Sampling/episode limitation resolved now, not deferred to another prompt:** preserve the complete existing 65-unit named purposive inventory and its accepted exact equivalences. Do not fabricate a retrospective sampling/rejection log or episode validation. The 71 catalog IDs do not establish >=40 adjudicated evidence-bearing episodes. Complete semantic/paraphrase recall, representative sampling and exhaustive dependency discovery are not certified by exact hash checks or the all-65 adjudication ledger. Consequently the paper is **exploratory, prespecified finite-benchmark simulation**, not full-design confirmatory completion. Existing groups remain descriptive dependency clusters, not independent incidents. This explicit narrowing is preferable to inflating counts or inventing review provenance. No additional annotation search is required today.

Pilot GRP-02 remains development; the 38/27 and 34/23 counts replace old 34/31 and 30/27 counts. Alternative-specific eligibility replaces the old 64/65 mask. The corrected raw-byte projection and PROP-08 identity language are retained. Two directly inconsistent historical adjudication notes (-04 asserting actual cleanup detection and -09 asserting independent agent identity) are corrected to observed warning/self-report language; no quotation, critical flag, core support or claim tier changes.

### Canonical benchmark pins

Git object IDs below identify canonical **blob bytes**, not working-tree CRLF content. SHA-256 is computed over `git cat-file blob <OID>`; OID is Git's blob identifier, not that SHA-256. Modified files were materialized with `git hash-object -w --path=<path> <path>` before pinning. These are the blobs to commit, not stale HEAD versions. Sam must compare the final committed tree to all pins.

| Artifact | Git blob OID | SHA-256 of canonical blob bytes |
|---|---|---|
| `annotations/evidence.jsonl` | `5cc077ff284279d61f2814beadcb66294a2569be` | `2efff9113056a797614de940c28b3fe1f5aac301fdaf3250c8fc83978c88565b` |
| `annotations/occurrences.jsonl` | `16aa2e42dd4bc94e86ba2a47382af4e546b68a46` | `02542d8e78b640da45f05cc1376c92226c459c44cdcbc1f782e022b932ad0c9d` |
| `annotations/eligibility.jsonl` | `8d55e351507bbf666f29a442fe3903137098144a` | `acb0ef84d749e42f9bac2786b1ceb7903a76bfdeae930eac98341109788f674a` |
| `annotations/splits.json` | `0e8d40fc4a13126cb62601a6c0f2813a22fb320e` | `46db260749340a6a37bc08bf7b1e6b5f1b386cc99063971e1b5f37c66eca84c9` |
| `annotations/adjudication.csv` | `5606d1e103e0bdb71c49e6ad8590054c1c4e1f0a` | `aaab09f6afae45b19b97b232d52ccdf827572715edb5638edfcad2322471fcf1` |
| `annotations/context_eligibility.jsonl` | `6c8ff9eac10e3465228bb4b3105f4df428d45cdb` | `59affb8ad353e87b98b00be21b75f22934a4c8eb3025e9384d31e79e7ba38034` |
| `annotations/context_fragments.jsonl` | `4a3b9bb2cd316878d65f6de696d37680c21ccd50` | `485e8897dbd9c8bea51ef1e2ec000653612b5539122b52db820e9945317848bd` |
| `annotations/context_occurrences.jsonl` | `bf386b68067bbb869325e98c982d4b555bc37ba1` | `040a4c8c5bff7b34428212470b1b036feab9331f02c4704a0ba80c5beefaa2f3` |
| `annotations/rubric.md` | `89b15d8d409ee1a9fb3ec6fa49a526880768efc2` | `a4e8b86ffc9971d75908ed95978636b9f782ef6f2f5b44b29d04eb41d415e76d` |
| `annotations/A11_BENCHMARK_SPEC.md` | `f2e87f34cbb30c7235d16a72ff188244ea55da73` | `d9deac839b2a3972af50ad1f4aa4f99cc2746e2178fe14a7cf5f3d12ee3bd276` |

No annotation semantic field may change after outcome access except a publicly disclosed post-outcome correction retaining original artifacts, results, reason, author, timing and impact. Correcting a genuine pre-run implementation bug does not authorize semantic retuning. No automatic regeneration may overwrite these frozen annotations.

## 2. Final E12 contract — implementation, not methodological redesign

Reviewed current `src/ebe/evaluator.py`, `src/ebe/a11_loader.py`, Sam's full `audit/E12_INDEPENDENT_ACCEPTANCE.md`, subsequent repair/status records and the context independent checks. The 19/19 independent hand-scored core mechanics are accepted prior evidence, not final acceptance of untested integrations. Current code contains retained-body SHA validation, post-checkpoint body rejection, DSE fragment namespace checks, body-required core alternatives, raw-body Latin-1 reversal and alternative exclusion. Do not re-report those as absent. Cross-title core loading and nominal earliest support are also repaired.

Remaining concrete issues: context files are not loaded; `context_covered` can satisfy a context-only bundle; low-level mixed-empty context handling collapses unknowns into misses; event matching is title/action only; feed times and packet/object/cap provenance are not fully validated; the loader slices occurrence quotes from the raw projection instead of consistently mapping canonical Unicode offsets. Existing real spans happen to pass checks, but the non-ASCII contract must work generally. No evaluator code is rewritten in this methodology handoff.

### Required public interface (freeze)

Keep `load_a11_benchmark(...) -> Benchmark` and `evaluate_benchmark(benchmark, snapshot, *, split='full', metric='core', critical_only=False) -> EvaluationResult`. Add explicit accepted `context_eligibility_path`, `context_fragments_path`, `context_occurrences_path`, `freeze_manifest_path` keyword arguments, defaulting only to these pinned accepted files. Real scoring requires `fail_on_error=True`; diagnostic loading with errors must never yield a scoreable benchmark. DRAFT basenames, unpinned hashes, unknown schema versions, duplicate/orphan IDs or incomplete dispositions fail closed, not a fallback loader. Retain dev/held_out/full with full meaning pooled descriptive, never fallback primary.

`Benchmark` must carry immutable propositions/fragments, all artifact hashes and per-proposition `context_state` in `self_contained|required|unknown|unavailable`. Global `context_status=FROZEN` means the dispositions are final, not that unknown context is automatically zero. All current states are self_contained/required. Freeze explicit extra-context DNF as follows:

- In eligibility, `context_alternatives` is authoritative where present; its flattened IDs must equal `context_fragment_ids`.
- Otherwise a context-needed row's nonempty `context_fragment_ids` is one AND alternative. Empty required IDs without explicit unknown/unavailable disposition are an error.
- Self-contained: context-complete DNF equals eligible core DNF, no artificial feed requirement.
- Required: for each eligible core alternative C and extra-context alternative X, form `C union X`. Fragment occurrence alternatives are OR; every component of an alternative is AND. No empty alternatives. Every expanded context alternative contains a complete eligible core alternative. Validate this AND enforce `context_satisfied => core_satisfied` at every public/helper scoring path; direct helpers cannot bypass it.
- PROP-61 is `(core AND ctx01) OR (core AND ctx02) OR (core AND ctx03)`, with all accepted core/occurrence alternatives expanded. Neither requiring all three nor choosing a preferred one is allowed.

Body context satisfaction uses only `context_occurrences.jsonl`: exact page, canonical body hash and canonical span bytes. Later carryforwards satisfy at their own acquisition times. Core likewise compiles only exact frozen occurrences, all required source spans together, with per-alternative eligibility before OR construction. Validate source SHA over `body.encode('latin-1')`, decode the declared codec strictly, map source-projection and Unicode coordinates explicitly, then canonical UTF-8. Do not UTF-8 encode the raw projection. No fuzzy expansion or source-private reconstruction can supply missing retained bytes. Apply checkpoint support eligibility independently of collector capture.

`Fragment` for observable events must carry exact page/action/event_time/minimum_multiplicity. Match the full frozen tuple against charged delivered feed records, with delivered_time<=checkpoint and event_time<=delivered_time; validate UTC-aware times and multiplicity. Wrong-time same-title deletions fail. Private event IDs are validation-only source references and cannot discriminate indistinguishable feed notifications. Under this amendment chronology is nominal; if a future separately amended trajectory changes event clocks, it must transform predicates and observable events consistently, not silently use private IDs.

`RetainedSnapshot` is the exclusive retained-at-C input. Extend the store export/adapter to include charged canonical packet bytes (including request_seq and archive_key where applicable), referenced body objects, cap, accounting totals, charged delivered feed/directory/archive context, and checkpoint. Validate packet schema/width, title/time/hash/object references, exact object bytes, refcounts, duplicate sequence IDs, no unreferenced hidden body, S<=cap, canonical UTF-8 and synchronized declared totals. Reject invalid snapshots; do not turn corruption into misses. `from_collector_result` may only use this store-derived export and charged delivered context, never body_results/capture_attempts/source store/unrestricted logs. The provenance matcher may compare retained bytes with frozen source data, never add content or context. Validate once per immutable snapshot/benchmark rather than rehashing every body for every fragment.

Ever-admitted-by-C uses a separate write-only admission/provenance journal, invisible to collectors and retained scoring. Oversize-rejected downloads are not admitted. An optional ever-downloaded diagnostic is separate. Preserve the firewall even if old audit fields remain for tests.

`EvaluationResult` must add `unknown_evidence` with reason codes and exact lower/upper context numerators; output covered/uncovered/unknown/excluded IDs distinctly. Core and context use the **same scoped eligible denominator** (K for held-out critical). Unknown/unavailable context is a per-unit NA, not uncovered zero and not a denominator deletion; if any exist, single point percentage is NA and report annotation-only interval `[known_complete/N,(known_complete+unknown)/N]`. Self-contained context misses exactly when core misses. Current known-groundable misses are genuine zeros, not unknowns. Missing/invalid data aborts, not NA annotation status. Empty denominator stays NA_EMPTY_DENOMINATOR; NOT_FROZEN is never a score.

Delay retains earliest completion of an entire retained core bundle (max within AND, min across OR), minus earliest eligible support. Use actual retained packet capture times, never original revision time as acquisition. Unretained is censored/failure, earliest support unknown is NA, not zero. Negative delay is an integrity/eligibility error, not clamped. Expose witness packet/fragment IDs and exact numerator/denominator integers. Retained, ever-admitted and rejected/evicted diagnostics remain separate.

## 3. Deadline experiment decision and exact matrix

**Option 2: FINAL_PRE_X13_DEADLINE_v1**, formal amendment at the top of `X13_RUN_CONTRACT.md`. Rationale: 100,319 original logical rows plus range expansion and still-missing engineering are an unjustified same-day commitment. This is an outcome-blind design decision, not measured favorable runtime/coverage pruning. No optional post-result expansion today.

All rows replay full held DSE background from May24 to July15, nominal chronology, 60s feed polls, FIFO/R04 bytes. Default cap1MiB, lag5s, GET0s. All periodic settings use all60 phases; E once, phase NA.

| Block | Exact roster | Rows |
|---|---|---:|
| L-primary | P15/PD15/PCD15 x60; E30/100/300 | 183 |
| L-interval | PCD5/PCD60 x60 | 120 |
| L-latency | PCD15 x60 + E30 at (lag,delay)=(5,30),(60,0),(60,30) seconds | 183 |
| F | final live only at 1MiB | 1 |
| R | online PCD-R15 x60, primary settings | 60 |
| O | reverse-title P15/PD15/PCD15 x60 + E30, primary settings | 181 |
| A-live | terminal archive on PCD15 x60 + E30 primary prefixes, 1MiB | 61 |
| A-only | archive-only at 1MiB and uncapped | 2 |
| **Total** | **Required logical collection/scoring rows** | **791** |
| U-stable | re-score primary PCD15 x60 + E30 using conservative interval mask | **61 families; no new collection rows** |

Primary unchanged: `D=100*(E30_core(K)-mean_60(PCD15_core(K)))`, K=23, rational threshold>=10pp. All-phase admissibility requires E30 requests<=minimum of all60 PCD15 request counts and identical M/access at same cap/interface/trajectory. No matching to mean, phase subset or realized occupancy. Partial admissibility is PRIMARY_UNMATCHED; show descriptive D and every flag. Secondary q never replaces E30. Labels for tie, PCD reversal, below-threshold, threshold met, unmatched, empty denominator and incomplete remain the v0 contract's exhaustive labels, qualified by exploratory finite-benchmark status.

Essential sensitivities: R, O, the full declared 2x2 lag/delay factorial including primary cell, A-live plus uncapped A-only recovery ceiling, nominal ambiguity and U-stable. **These are not optional today.** Stable mask is deterministic v0 §5 support identity/availability common to all closed +/-u states; uncertifiable support excluded, common background retained. Generate and hash compiled intervals before scoring. Use snapshot capture times against these intervals, not just source save times. Recompute common stable denominator and show removals; it is not a shared-world replay bound.

### What may be omitted / forbidden claims

The amendment explicitly removes July3, broader live cap grid, Delta1, P/PD5/60, q100/300 outside primary, lag30/delay5, secondary R/O/A cross-products, P/PD archive prefixes, other capped archives, and full nominal-tie/+/-u trajectory enumeration/certification. Every removed original row/family must appear in `omitted_scope.csv` as NOT_EXECUTED_SCOPE_AMENDMENT. Full-range fields are NA, not sampled bounds or zero. This is a deliberate design omission, not an alleged solver result. Standalone secondary graphics may be omitted as defined in the amendment; tables/disclosures cannot.

Allowed paper claim: **an exploratory prespecified finite-benchmark, all-phase scheduling contrast under a shared hypothetical feed and declared nominal live-only simulation, conditional on the released DSE history and request/cap constraints**, accompanied by all executed sensitivities and costs. No full-design confirmatory result, representative incident population, independently validated 71 evidence-bearing episodes, chronology-robust result, broad-cap/checkpoint/rate robustness, general event-driven superiority, permanent erasure, historical responder loss, historical archive availability, same total cost, or optimal collection claim. A disappearing gain against PCD-R limits it to changed-only PCD; archive collapse must sit beside the live-only conclusion. If any essential block cannot finish, publish incomplete descriptive rows only, not a completed amendment/headline decision. Deadline is not authority to suppress an unfavorable result or waive a missing implementation.

## 4. ONE implementation checklist for Ubayd

Create the files named below (or update the existing named files), implementing only this frozen contract. All tests before Gate2 are synthetic or data-only: no real policy/annotation join.

### A. Observer, clock and storage integrity

Update `src/ebe/observer.py`, `src/ebe/collectors.py`, `src/ebe/storage.py`, `src/ebe/accounting.py`.

- X01: general `costs(C)` excludes dispatches/polls/directories after C even on a longer execution history. Count [t0,C) continuous dispatches, completions through C, pending after C, terminal feed poll at C; no sweep/E dispatch at C. Future headers/payloads cannot affect as-of totals. Fresh-prefix and longer-run as-of results agree wherever the permitted terminal abstraction is identical.
- X02: one monotone operation clock across polls, GET dispatch, completions and terminal operations. Neither poll-after-future-GET nor GET-after-future-poll may backdate knowledge. Same-time order: source mutations, feed publication/delivery, due completions/store admissions, then new sweep/E service; zero-delay responses admitted in request sequence. A request at C is legal only for the explicitly atomic F/archive terminal abstraction.
- Terminal directory one-shot at T, no feed mixing, state-derived live/BU membership; no enumerating 5,154 future title keys as initial discovery. Directory-discovered GET cannot be backdated, repeated directory rejected. Ambiguous membership fails explicitly, not title selection.
- Delayed `get_body` returns opaque pending handle, not a future body/outcome; release payload only through completion at availability time. Diagnostics must not expose future body/headers to policy decisions. Pending at C has a charged attempt but no invented transfer.
- Safe retained packet/object export, synchronized S/M/S+M temporal ledger, exact integral byte-hours and peak synchronized sum; R04 standalone oversize-before-eviction, FIFO/refcounts/no zero-reference body cache unchanged. Admission journal separated from retention and policies.

Create `tests/test_final_observer_contract.py`: both backward operation directions, repeated terminal directory, directory+feed mixing, terminal backdating, C-1us/C/C+1us dispatch/completion/poll boundaries, delayed unavailable payload, longer-run as-of and fresh-prefix agreement, non-ASCII/empty-known/unknown body cases and synthetic cost hand counts.

### B. Collectors and essential hostile controls

Create `src/ebe/terminal_archive.py`; update `src/ebe/collectors.py`.

- X03 F: one charged terminal state directory at T, lexicographic GET every listed live/BU title, atomic zero delay, dedup/cap, no feed. Missing/unknown stays explicit, no source revision preload.
- PCD-R15: implement **exact v0 §3** online repair using last completed known hash/length only, per-title retained provenance, current-body GET, sweep-start candidate union, pending exclusion and prospective actual sequence packet-width fit test. Ordinary dirty work cannot be size-skipped. Unavailable completion disables repair until another known completion. No off-sweep retry, body cache or same-sweep repair feedback; instrument metadata overhead. Cap-dependent repair schedules cannot be shared across caps.
- Ordering: reverse title service at sweeps; E reverses title tie-break only, not event-time priority or chronology. Feed/source order unchanged; actual admission order determines FIFO.
- Archive: **exact v0 §7** persistent terminal model. One global enumeration of every held DSE revision saved<=T, sorted (event_time,page_key,archive_key), then GET every entry. Metadata records only opaque rev ID/page/time, retained in M; response array framing charged separately. Five-field archive packets with capture_time=T, archive_key and continuing request_seq; same existing store/cap/dedup/FIFO, no protected live partition. Archive-only starts empty seq1. Cost all requests, responses, metadata, evictions and unknowns; atomic terminal activity has zero pre-T byte-hours, not zero retrieval cost. Pin hand-counted fixtures for enumeration and archive packets. Report pre/post archive beside each other; match total live+archive costs under shared access.

Create `tests/test_final_controls.py` and `tests/fixtures/final_x13_controls.json`: F hand-counted directory/GET; retained-other-title shared body cannot repair provenance for free; oversize width boundary, pending and same-instant completion repair; reverse-order FIFO differences; full-background archive enumeration, repeated-body GET charging, capped evictions and uncapped ceiling costs. Fixtures use invented titles/text only.

### C. E12 and frozen data compilation

Update `src/ebe/a11_loader.py`, `src/ebe/evaluator.py`, `docs/E12_EVALUATOR.md` to §2 above. Create `src/ebe/support_masks.py`, `tests/test_final_e12_contract.py`, `tests/test_support_masks.py`.

Tests must include core missing/context retained => incomplete, each Ivy-shaped OR alternative independently sufficient only with core, multi-fragment AND missing one, mixed known/unknown/unavailable context never coerced to zero, self-contained context=core, wrong-time same-title event rejected, undelivered/future event rejected, multiplicity, rejected/evicted/future diagnostic poison, missing object/refcount/cap/hash/packet corruption, DRAFT/hash mismatch rejection, both encoding coordinate systems with non-ASCII before the quote, empty known body distinct from BU, cross-title alternatives and head-mismatch anchor with a good other-page alternative. Retain Sam's 19 mechanics cases with real fixture hashes; do not weaken tests to accept broken data. Stable masks must test uncertain interval endpoints and equal-text certification, incompatible/unsupported chronology and checkpoint eligibility. Required output `freeze/stable_support_intervals.jsonl` and its hash are generated from frozen source/algorithm **before scoring**, never by looking at captures or labels' policy performance.

### D. Runner, manifest, reproducibility and costs

Create:

- `configs/x13_deadline_v1.json` — exact 791-row recipe, immutable numeric settings and source/benchmark/contract identities.
- `src/ebe/x13_manifest.py` — data-only generation/validation of full logical manifest plus all omitted original v0 rows/families.
- `src/ebe/x13_runner.py` — offline run/resume driver, scoring disabled until independent PASS matches code+manifest hashes.
- `src/ebe/x13_reporting.py` — exact rational aggregation, CSVs and deterministic plots from frozen CSV only.
- `tests/test_x13_manifest.py`, `tests/test_x13_runner_synthetic.py`, `tests/test_x13_reporting_synthetic.py` — row counts, primary identity, phase formulas, omitted rows, invariants, interrupted/resumed equality and all failure/NA labels with hand-scored invented data.
- `scripts/reproduce_x13.py`, `docs/X13_REPRODUCIBILITY.md` — offline clean-environment entry point, dependency/environment instructions; no network/model calls, URL execution or raw redistribution. Reproduction before authorization uses synthetic fixtures only.
- `audit/UBAYD_FINAL_ENGINEERING_READINESS.md` — checklist, commands, measured synthetic runtime/memory feasibility, unresolved bugs (must be none for PASS), source/code/environment pins and exact candidate commit. Estimate same-day feasibility without semantic results; do not promise actual completion. A resource failure later leaves explicit incomplete rows.

Write generated immutable pre-score files beneath `runs/FINAL_PRE_X13_DEADLINE_v1/freeze/`: `benchmark_manifest.json`, `configuration_manifest.csv`, `omitted_scope.csv`, `stable_support_intervals.jsonl`, `run_manifest.json`, `manifest.sha256`. Hash the canonical immutable manifest BEFORE any scoring; it includes hashes of the other frozen files, source export/normalization/accounting fixtures, code commit, environment/lock and all ten annotation pins. No self-referential hash; dynamic status/runtime logs live outside freeze/. Gate2 pins this manifest in its report.

Configuration serialization: canonical sorted-key compact UTF-8 JSON with integer microseconds, literal null uncapped/NA phase, no floats for clocks. Logical run ID is SHA256 of canonical `{amendment,block,config,source_hash,code_commit,benchmark_hashes,mask_hash}`; separate computation/cache ID may omit block only after equivalence proof. Execution sort uses v0 §8 order. Resume only when every scientific hash matches; atomic per-row output+checksum and append-only status log. Preserve prior attempts and exact errors; no overwrite hiding failed runs. Label-independent immutable source/feed parsing may be shared. Request-stream reuse needs tested schedule/cap/ordering/latency invariance; storage replay remains per config, PCD-R requires online store decisions, and archive state must continue the exact prefix. A checkpoint cannot reuse future state; runtime/RSS for reused rows is NA/reused_from, not a separately measured policy runtime. No scoring feedback reaches caches or collection scheduling.

Each required logical row remains present with PLANNED/RUNNING/COMPLETE/ERROR/MISSING/NOT_EVALUABLE plus reason and reuse link. Scope removals are a different status/file. An `unsupported` nominal background response invalidates that run because FIFO spillovers can affect any proposition. Unknown historical/BU outcomes specified by the source model are distinct, not automatically implementation failures.

All §6 costs required, not just requests: attempts decomposed feed/directory/live-body/archive; completed responses by outcome; pending/outstanding; response header/feed/directory/archive metadata bytes; known-body/download total and known lower bound with NA payload count; final/peak S,M,S+M and body/packet decomposition; synchronized byte-hours; objects/refcounts; evicted body/packet bytes; oversize rejection; dirty/pending/index peaks, coalescing/starvation/dropped work. Instrument CPU/process time/elapsed/RSS and artifact disk bytes, logical auxiliary memory; separate source/model/collector/store/evaluator scope where measurable. No missing payload/overhead imputed as zero; simulator runtime is not deployment advantage.

### E. Evaluation and reporting outputs

Under `runs/FINAL_PRE_X13_DEADLINE_v1/results/`, create all these UTF-8 CSVs plus a machine-readable status index: `completeness.csv`, `all_configurations.csv`, `headline.csv`, `admissibility.csv`, `cost_envelopes.csv`, `phase_values.csv`, `latency_checkpoint.csv`, `baseline_robustness.csv`, `group_metrics.csv`, `group_robustness.csv`, `unit_retention.csv`, `delay.csv`, `eligibility_missingness.csv`, `unknowns.csv`, `chronology_sensitivity.csv`, `archive_sensitivity.csv`, `case_walkthrough.csv`, `overhead.csv`, `reproducibility.md`. These are NOT to be generated from real captures until authorization.

Every executed row: dev/held-out/pooled overall body core, critical core, context-complete on corresponding same denominator, moderator-event-only status NA/not in body benchmark, retained/ever-admitted and delay status/counts, witnesses, exclusions/unknowns and costs. Distinct moderator events never inflate K. Publish per-unit retention only in the authorized run, never to annotation/collector decisions.

All60 phase values plus j0/mean/min/max/p10/median/p90 with linear `(n-1)*p` quantiles; E appears once. All-phase request admissibility, gaps and phase flags for every generated comparable E/periodic family. Equal-proposition micro, group macro and leave-one-group-out primary range with zero-critical groups explicit; no bootstrap, p-value, population confidence interval or treating phases as independent incidents. Delays conditional on retained fraction, never unretained=zero. Group/phase summaries do not replace missing raw rows.

Required SVG+PNG from CSV: `coverage_cost`, `phase_distribution`, `latency_checkpoint`, `baseline_robustness`, `archive_sensitivity`, `case_walkthrough`; chronology may be a table with explicit omitted-range NA fields and stable-mask results. Same 0–100 coverage axes, signed differences include zero and +/-10pp, hollow unmatched points, no omitted-cap connecting lines or zero-plotted NA. Fixed construction+ZZZ development walkthrough, no replacement case or chosen phase. Headline table links costs/overhead and repair/archive limitations adjacent, not hidden in supplement.

Result guardrails: **no q promotion; no favorable phase choice; no label-dependent reruns/tuning; no silent failed-row deletion.** A genuine bug correction requires preserved original attempts, changed code identity, public reason and rerun of every affected configuration by code dependency, not by favorable labels. No post-outcome annotation changes without disclosed correction. Null, reversal, below-threshold, unmatched and archive-collapse results are valid scientific outcomes, never grounds for another methodological redesign.

## 5. TWO gates only

### Gate 1 — Engineering readiness (Ubayd)

PASS requires every §4 implementation and required output schema, all synthetic/data-only tests green, all ten annotation hashes unchanged, correct 791-row manifest and explicit v0 omissions, pre-score stable mask and manifest hashes, no real scoring, documented offline synthetic resume/reproduction and cost/overhead instrumentation, no unresolved unsupported nominal engine behavior. Readiness report names code commit and immutable manifest SHA. Ubayd's PASS is an engineering assertion, not independent approval. A failed test is repaired within this gate; do not issue separate methodology micro-prompts.

### Gate 2 — Independent readiness (Sam)

Sam creates `audit/FINAL_PRE_X13_INDEPENDENT_READINESS.md` and `audit/verify_final_x13_readiness.py`. One comprehensive audit of the exact Gate1 commit/manifest: independently recompute benchmark pins/counts/references, verify pilot/alternative eligibility/context OR semantics and frozen event predicates; hand-score fresh adversarial E12/observer/control/cost fixtures with expected answers written first; verify no diagnostic/source/label leak, full background, cap/refcount/byte-hours integrity, all logical rows/omissions and deterministic resume/reproduction. Inspect candidate implementation, not just author PASS claims. Retain/disclose Sam's prior substrate/test authorship and the earlier checker hash mistakes. Re-running existing scripts is supporting evidence, not sole independence. No real policy coverage is needed or authorized for this audit.

Sam's **PASS**, with exact code/source/annotation/config/manifest hashes and zero unresolved violations of this document, simultaneously grants final independent E12 PASS and **authorizes X13 for that exact manifest**. No further broad Astra methodology review or separate third approval is required. If Sam finds a genuine methodological contradiction, stop and disclose it; ordinary implementation bugs return to Ubayd within these same two gates. Changes after PASS invalidate authorization for changed scientific bytes until Sam checks the affected implementation; this is not a new broad review sequence.

## 6. Consolidation verification actually performed

- Complete read of all37 ordered-ID matching revision bodies and both raw deletion events; all three initial helper hashes/spans rederived from raw bytes.
- `python audit/verify_final_context.py --generate`: created the new exact context census using a full-DSE search; 101 body occurrences, no new cross-page matches; all ten fragment references verified. Default verify mode is read-only.
- `python audit/verify_annotations.py`: 65/65 evidence, 1,421/1,421 occurrences, split isolation and 65 adjudication rows PASS.
- `python audit/verify_a11_independent.py`: zero failures on current annotations; this execution by the consolidator is not a new Sam signoff.
- Structural `load_a11_benchmark()` only: valid; 65 propositions, 1,420 compiled core span fragments, nominal K23, 65 earliest support timestamps. No retained snapshot supplied and no coverage/delay evaluated.
- No claim of current engineering or independent readiness PASS. The remaining work is precisely §4 and the two gates, not another list of annotation-methodology decisions.

### READY FOR FINAL ENGINEERING PASS
